services:
  - type: web
    name: eaglercraft-server
    env: java
    region: oregon
    plan: free
    buildCommand: echo "Build complete"
    startCommand: java -Xmx512M -jar server/eaglercraft-server.jar nogui
    envVars:
      - key: PORT
        value: 25565
